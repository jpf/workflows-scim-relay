{
  "name": "workflows-scim-relay",
  "version": "0.5.0",
  "devDependencies": {
    "jest": "^29.5.0",
    "wrangler": "^2.20.0"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler publish",
    "test": "jest"
  }
}
